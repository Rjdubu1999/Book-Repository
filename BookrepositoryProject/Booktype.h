#pragma once
#include <string>

enum Booktype { UNDECIDIED, FICTION, NONFICTION };

/*Parallel array makes string for each book type*/
static const std::string bookTypeStrings[] = { "UNDECIDED","FICTION","NONFICTION" };
